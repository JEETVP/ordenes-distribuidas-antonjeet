services:
  api-gateway:
    ports:
      - "80:8000"
    environment:
      LOG_LEVEL: WARNING
    restart: always

  auth-service:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  writer-service:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  notification-service:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  postgres:
    restart: always

  postgres-notifications:
    restart: always

  redis:
    restart: always

  rabbitmq:
    image: rabbitmq:3-alpine
    restart: always
