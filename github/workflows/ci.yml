name:CI #Nombre del workflow
on: [push,pull_request] #Evento que dispara el workflow
jobs:
  test: #nombre del job
    runs-on: ubuntu-latest #Runner (maquina virtual) donde se ejecutará el job
    steps:
      -uses:actions/checkout@v4 #Paso 1, para clonar el repositorio
      -run:echo "Hello Anton" #Paso 2, para imprimir un mensaje en la consola

